/**
 * @deprecated Use `SiteFooter` from `@/components/ui/footer` — the same
 * primary footer as /products and every other public route.
 */
export { SiteFooter as HomeFooter } from "@/components/ui/footer";
